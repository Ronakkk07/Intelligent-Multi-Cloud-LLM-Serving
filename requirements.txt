# Core router
httpx>=0.27.0
pyyaml>=6.0.1
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Datasets
datasets>=2.19.0
huggingface-hub>=0.23.0

# Tokenizer (optional — falls back to word-count if not installed)
tiktoken>=0.7.0

# Telemetry / observability
redis>=5.0.4
prometheus-client>=0.20.0

# Cloud SDKs
boto3>=1.34.0
azure-mgmt-costmanagement>=4.0.0
azure-identity>=1.16.0

# Load testing
locust>=2.29.0

# Data / stats
pandas>=2.2.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
